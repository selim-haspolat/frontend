const Loading = () => {
  return (
    <div className="w-full h-full flex justify-center items-center grow">
      <div class="border-gray-300 h-16 w-16 animate-spin rounded-full border-8 border-t-primary" />
    </div>
  );
};

export default Loading;
